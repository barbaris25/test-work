import React from 'react'
import { Form } from '../components/Form'

export const AddUser = ({ props }) => {
  return (
    <div className="row">
      <Form props={props} />
    </div>
  )
};
